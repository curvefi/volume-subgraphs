{
  "name": "volume-sidechains",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ convex-community/volume-avalanche",
    "create-local": "graph create --node http://localhost:8020/ convex-community/volume-avalanche",
    "remove-local": "graph remove --node http://localhost:8020/ convex-community/volume-avalanche",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex-community/volume-avalanche"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
