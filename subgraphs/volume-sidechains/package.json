{
  "name": "volume-sidechains",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ convex-community/volume-sidechains",
    "create-local": "graph create --node http://localhost:8020/ convex-community/volume-sidechains",
    "remove-local": "graph remove --node http://localhost:8020/ convex-community/volume-sidechains",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex-community/volume-sidechains"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
