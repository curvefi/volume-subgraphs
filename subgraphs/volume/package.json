{
  "name": "volume",
  "license": "MIT",
  "version": "1.0.0",
  "files": [
    "generated"
  ],
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ curvefi/curve-volume",
    "deploy-hosted": "graph deploy --product hosted-service convex-community/curve-volume",
    "deploy-staging": "graph deploy --product hosted-service convex-community/curve-volume-staging",
    "deploy-studio": "graph deploy --studio curve-volume",
    "create-local": "graph create --node http://localhost:8020/ convex/curve-volume",
    "remove-local": "graph remove --node http://localhost:8020/ convex/curve-volume",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 curvefi/curve-volume"
  }
}
